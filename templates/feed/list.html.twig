{% for feed in feeds %}
    {% include 'feed/item.html.twig' with { feed } %}
{% else %}
    <tr>
        <td colspan="4">no records found</td>
    </tr>
{% endfor %}

<div class="container">
    <div class="row">
        <div class="d-flex align-items-center justify-content-center">
            {{ knp_pagination_render(feeds) }}
        </div>
    </div>
</div>