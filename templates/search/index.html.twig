{% extends 'base.html.twig' %}

{% block title %}My search list{% endblock %}

{% block body %}
    <main class="container">
        <div class="row">
            <div>
                <h1 class="pb-4 mb-4 border-bottom">My search list</h1>
    
                <table class="table">
                    <thead>
                        <tr>
                            <th>Name</th>
                            <th>actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% include 'search/list.html.twig' with { searches: searches } %}
                    </tbody>
                </table>

                <a href="{{ path('app_search_new') }}" class="btn btn-primary">Create new</a>

            </div>
        </div>
    </main>
{% endblock %}
