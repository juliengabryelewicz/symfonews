{% extends 'base.html.twig' %}

{% block title %}New Search{% endblock %}

{% block body %}
<main class="container">
    <div class="row">
        <div>
            <h1 class="pb-4 mb-4 border-bottom">Create new Search</h1>

            <div class="form mb-2">
                {{ include('search/_form.html.twig') }}
            </div>

            <a href="{{ path('app_search_index') }}" class="btn btn-primary">back to list</a>
        </div>
    </div>
</main>
{% endblock %}
