{% for search in searches %}
    {% include 'search/item.html.twig' with { search } %}
{% else %}
    <tr>
        <td colspan="4">no records found</td>
    </tr>
{% endfor %}

<div class="container">
    <div class="row">
        <div class="d-flex align-items-center justify-content-center">
            {{ knp_pagination_render(searches) }}
        </div>
    </div>
</div>