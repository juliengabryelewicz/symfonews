{% extends 'base.html.twig' %}

{% block title %}Edit Search{% endblock %}

{% block body %}
<main class="container">
    <div class="row">
        <div>
            <h1 class="pb-4 mb-4 border-bottom">Edit Search</h1>

            {{ include('search/_form.html.twig', {'button_label': 'Update'}) }}

            <a class="btn btn-primary" href="{{ path('app_search_index') }}">back to list</a>

            {{ include('search/_delete_form.html.twig') }}
        </div>
    </div>
</main>
{% endblock %}
