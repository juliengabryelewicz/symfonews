{% for favorite in favorites %}
    {% include 'favorite/item.html.twig' with { favorite } %}
{% else %}
    <article class="mb-5">
        <div class="row">
            No favorite found
        </div>
    </article>
{% endfor %}

<div class="container">
    <div class="row">
        <div class="d-flex align-items-center justify-content-center">
            {{ knp_pagination_render(favorites) }}
        </div>
    </div>
</div>